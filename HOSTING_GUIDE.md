# E-Sekoir - دليل الرفع للاستضافة الخاصة

## ✨ المميزات الجديدة

- ✅ **اكتشاف تلقائي للدومين** - لا تحتاج لتعديل SITE_URL
- ✅ **CORS تلقائي** - يعمل على أي دومين
- ✅ **متجاوب مع الهاتف** - تصميم محسّن للموبايل

## 📁 هيكل الملفات المطلوب رفعها

```
public_html/
├── api/                          ← مجلد الـ API (PHP)
│   ├── config/
│   │   └── database.php          ← ⚠️ عدّل بيانات قاعدة البيانات فقط
│   ├── auth/
│   ├── currencies/
│   ├── comments/
│   ├── profiles/
│   ├── admin/
│   └── .htaccess
│
├── database/
│   └── install.php               ← شغّله مرة واحدة ثم احذفه
│
├── assets/                       ← من مجلد dist/assets
├── icons/                        ← من مجلد public/icons
├── index.html                    ← من مجلد dist
├── robots.txt                    ← من مجلد public
└── .htaccess                     ← للـ SPA routing (أنشئه)
```

## 🔧 الملفات التي تحتاج تعديل

### 1. `api/config/database.php` - فقط بيانات MySQL

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'اسم_قاعدة_بياناتك');
define('DB_USER', 'اسم_المستخدم');
define('DB_PASS', 'كلمة_المرور');
define('JWT_SECRET', 'مفتاح_سري_طويل_وعشوائي');
```

**ملاحظة:** SITE_URL و ALLOWED_ORIGINS يتم اكتشافهم تلقائياً الآن!

### 2. أنشئ `.htaccess` في public_html

```apache
RewriteEngine On
RewriteBase /
RewriteRule ^api/ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

## 📋 خطوات الرفع

### 1. بناء المشروع
```bash
npm run build
```

### 2. إنشاء قاعدة البيانات في cPanel
- أنشئ قاعدة بيانات MySQL
- أنشئ مستخدم وامنحه كل الصلاحيات

### 3. رفع الملفات
| المصدر | الوجهة |
|--------|--------|
| `dist/index.html` | `public_html/index.html` |
| `dist/assets/` | `public_html/assets/` |
| `public/icons/` | `public_html/icons/` |
| `api/` | `public_html/api/` |
| `database/install.php` | `public_html/database/install.php` |

### 4. تعديل database.php
عدّل بيانات MySQL فقط

### 5. تشغيل سكريبت التثبيت
زُر: `https://موقعك.com/database/install.php`
ثم **احذف الملف** للأمان!

### 6. اختبار
- الموقع: `https://موقعك.com`
- API: `https://موقعك.com/api/health.php`

## ⚠️ ملاحظات مهمة

- ❌ لا تنسَ حذف `install.php` بعد التثبيت
- 🔐 غيّر `JWT_SECRET` لقيمة آمنة
- ✅ النسخة الحالية تعمل على أي دومين تلقائياً

## 📱 التوافق مع الهاتف

الموقع محسّن للعمل على:
- الهواتف المحمولة (< 640px)
- الأجهزة اللوحية (640px - 1024px)
- شاشات الكمبيوتر (> 1024px)

---

**للمزيد من التفاصيل، راجع:** `UPLOAD_GUIDE.md`
